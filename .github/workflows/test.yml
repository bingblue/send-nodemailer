name: Test Action
on:
  push:
    branches:
      - master
jobs:
  test:
    name: 测试
    runs-on: ubuntu-latest
    steps:
    - name: 更新代码
      uses: actions/checkout@master
    
    - name: 发送邮件
      uses: ./
      with:
        myinput: '${{github.repository}}'
        myInput2: '123456'
